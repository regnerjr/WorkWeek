import UIKit
import XCTest

import CoreLocation

import WorkWeek

class TableViewControllerTest: XCTestCase {

//    let manager = CLLocationManager()
//    let region = CLCircularRegion(center: CLLocationCoordinate2D(latitude: 0.0, longitude: 0.0),
//        radius: 10, identifier: "WorkRegion")
//
//    //must set up a monitored region or the vc will try to segue to the map view
//
//    func testDidEnterRegion() {
//        let vc = TableViewController()
//        vc.locationManager(manager, didEnterRegion: region)
//        //vc has a work manager it should add an arrival
//        XCTAssertEqual(vc.array.count, 0, "1 arrival does not make a workday")
//    }
//
//    func testDidExitRegion() {
//        let vc = TableViewController()
//
//        vc.locationManager(manager, didExitRegion: region)
//        //adds an event
//        //the event is a departure
//        //self. array has 1 item a departure
//        XCTAssertEqual(vc.array.count, 0, "1 departure does not make a work day")
//    }
//    func testOneWorkDay(){
//        let vc = TableViewController()
//        vc.locationManager(manager, didEnterRegion: region)
//        vc.locationManager(manager, didExitRegion: region)
//        XCTAssertEqual(vc.array.count, 1, "1 arrival 1 exit makes 1 work event")
//    }
}
